# Migration `20191011162041-ondelete-cascade`

This migration has been generated by Serg Kizim at 10/11/2019, 4:20:41 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191011155143-init..20191011162041-ondelete-cascade
--- datamodel.dml
+++ datamodel.dml
@@ -7,24 +7,24 @@
   provider = "photonjs"
 }
 model User {
-  id        Int      @id
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
+  id        Int       @id
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
   name      String?
-  role      UserRole @default(EMPLOYEE)
-  accounts  Account[]
+  role      UserRole  @default(EMPLOYEE)
+  accounts  Account[] @relation(onDelete: CASCADE)
 }
 model Account {
-  id            Int      @id
-  createdAt     DateTime @default(now())
-  updatedAt     DateTime @updatedAt
+  id            Int           @id
+  createdAt     DateTime      @default(now())
+  updatedAt     DateTime      @updatedAt
   name          String
-  currency      Currency @default(USD)
+  currency      Currency      @default(USD)
   user          User
-  transactions  Transaction[]
+  transactions  Transaction[] @relation(onDelete: CASCADE)
 }
 model Transaction {
   id        Int      @id
```

## Photon Usage

You can use a specific Photon built for this migration (20191011162041-ondelete-cascade)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191011162041-ondelete-cascade'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
