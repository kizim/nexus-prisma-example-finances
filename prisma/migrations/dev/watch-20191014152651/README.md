# Migration `watch-20191014152651`

This migration has been generated by Serg Kizim at 10/14/2019, 3:26:51 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Account" (
  "balance" REAL NOT NULL DEFAULT 0  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "currency" TEXT NOT NULL DEFAULT 'USD'  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "user" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL
);

INSERT INTO "new_Account" ("createdAt","currency","id","name","updatedAt","user") SELECT "createdAt","currency","id","name","updatedAt","user" from "Account"

DROP TABLE "lift"."Account";

ALTER TABLE "lift"."new_Account" RENAME TO "Account";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20191014152651
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,47 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id        Int       @id
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  name      String?
+  role      UserRole  @default(EMPLOYEE)
+  accounts  Account[] @relation(onDelete: CASCADE)
+}
+
+model Account {
+  id            Int           @id
+  createdAt     DateTime      @default(now())
+  updatedAt     DateTime      @updatedAt
+  name          String
+  currency      Currency      @default(USD)
+  balance       Float         @default(0)
+  user          User
+  transactions  Transaction[] @relation(onDelete: CASCADE)
+}
+
+model Transaction {
+  id        Int      @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  amount    Float
+  account   Account
+}
+
+enum UserRole {
+  ADMIN
+  EMPLOYEE
+}
+
+enum Currency {
+  USD
+  EUR
+  RUB
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191014152651)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191014152651'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
