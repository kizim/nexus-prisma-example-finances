# Migration `20191015175407-init`

This migration has been generated by Serg Kizim at 10/15/2019, 5:54:07 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "email" TEXT    ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT    ,
  "password" TEXT    ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  
);

CREATE TABLE "lift"."Account" (
  "balance" REAL NOT NULL DEFAULT 0  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "currency" TEXT NOT NULL DEFAULT 'USD'  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "user" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL
);

CREATE TABLE "lift"."Transaction" (
  "account" INTEGER    REFERENCES "Account"(id) ON DELETE SET NULL,
  "amount" REAL NOT NULL DEFAULT 0  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  
);

CREATE TABLE "lift"."User_roles" (
  "nodeId" INTEGER    REFERENCES "User"(id) ON DELETE CASCADE,
  "position" INTEGER NOT NULL   ,
  "value" TEXT NOT NULL   ,
  PRIMARY KEY ("nodeId","position")
);

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191015175407-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,49 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id        Int         @id
+  createdAt DateTime    @default(now())
+  updatedAt DateTime    @updatedAt
+  name      String?
+  accounts  Account[]   @relation(onDelete: CASCADE)
+  email     String?     @unique
+  password  String?
+  roles     UserRole[]
+}
+
+model Account {
+  id            Int           @id
+  createdAt     DateTime      @default(now())
+  updatedAt     DateTime      @updatedAt
+  name          String
+  currency      Currency      @default(USD)
+  balance       Float         @default(0)
+  user          User
+  transactions  Transaction[] @relation(onDelete: CASCADE)
+}
+
+model Transaction {
+  id        Int      @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  amount    Float
+  account   Account
+}
+
+enum UserRole {
+  ADMIN
+  EMPLOYEE
+}
+
+enum Currency {
+  USD
+  EUR
+  RUB
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191015175407-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191015175407-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
