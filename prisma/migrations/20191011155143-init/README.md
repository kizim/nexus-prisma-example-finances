# Migration `20191011155143-init`

This migration has been generated by Serg Kizim at 10/11/2019, 3:51:43 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT    ,
  "role" TEXT NOT NULL DEFAULT 'EMPLOYEE'  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  
);

CREATE TABLE "lift"."Account" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "currency" TEXT NOT NULL DEFAULT 'USD'  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "user" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL
);

CREATE TABLE "lift"."Transaction" (
  "account" INTEGER    REFERENCES "Account"(id) ON DELETE SET NULL,
  "amount" REAL NOT NULL DEFAULT 0  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  
);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191011155143-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,46 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id        Int      @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  name      String?
+  role      UserRole @default(EMPLOYEE)
+  accounts  Account[]
+}
+
+model Account {
+  id            Int      @id
+  createdAt     DateTime @default(now())
+  updatedAt     DateTime @updatedAt
+  name          String
+  currency      Currency @default(USD)
+  user          User
+  transactions  Transaction[]
+}
+
+model Transaction {
+  id        Int      @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  amount    Float
+  account   Account
+}
+
+enum UserRole {
+  ADMIN
+  EMPLOYEE
+}
+
+enum Currency {
+  USD
+  EUR
+  RUB
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191011155143-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191011155143-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
